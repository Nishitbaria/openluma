services:
  db:
    image: postgres:17-alpine
    restart: unless-stopped
    ports:
      - "5432:5432"
    environment:
      POSTGRES_USER: openluma
      POSTGRES_PASSWORD: openluma
      POSTGRES_DB: openluma
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
